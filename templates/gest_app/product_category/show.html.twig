{% extends 'base.html.twig' %}

{% block title %}ProductCategory{% endblock %}

{% block body %}
    <h1>ProductCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_category.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ product_category.slug }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ product_category.createdAt ? product_category.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ product_category.updatedAt ? product_category.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gest_app_product_category_index') }}">back to list</a>

    <a href="{{ path('gest_app_product_category_edit', {'id': product_category.id}) }}">edit</a>

    {{ include('gest_app_product_category/_delete_form.html.twig') }}
{% endblock %}
