<form method="post" action="{{ path('gest_app_product_nature_delete', {'id': product_nature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_nature.id) }}">
    <button class="btn">Delete</button>
</form>
