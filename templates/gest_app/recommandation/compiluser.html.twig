<table class="table table-striped">
    <thead>
        <tr>
            <th colspan="2">Statistiques personnelle</th>
        </tr>
        <tr>
            <th>Statut</th>
            <th>Recues</th>
            <th>Envoyés</th>
        </tr>
    </thead>
    <tbody>
    {% for stat in stats %}
        <tr>
            <td>
                {% if stat.recoState == 'noRead' %}
                    Non lue
                {% elseif stat.recoState == 'InProgress' %}
                    En cours
                {% elseif stat.recoState == 'Valid' %}
                    validée
                {% elseif stat.recoState == 'Avort' %}
                    Sans suite
                {% endif %}</td>
            <td>{% if stat.recoPrice is not null %}{{ stat.recoPrice }} €{% else %}0.00 €{% endif %}</td>
            <td></td>
        </tr>
    {% else %}
        <tr>
           <td></td>
        </tr>
    {% endfor %}
    </tbody>
</table>