{% extends 'base.html.twig' %}

{% block title %}Edit MediaOne{% endblock %}

{% block body %}
    <div class="container-fluid">
        {{ include('webapp/media_one/include/_section_content_header.html.twig') }}
        <section id="op_admin_content_body">
            <div class="row g-2">
                <div class="col-2">
                    {{ include('webapp/media_one/include/_section_content_aside.html.twig') }}
                </div>
                <div class="col-10">
                    <div class="card">
                        <div class="card-header">
                            <h3>Modifier page</h3>
                        </div>
                        {{ include('webapp/media_one/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    </div>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
