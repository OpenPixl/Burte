<form method="post" action="{{ path('gest_app_event_gal_delete', {'id': event_gal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ event_gal.id) }}">
    <button class="btn">Delete</button>
</form>
