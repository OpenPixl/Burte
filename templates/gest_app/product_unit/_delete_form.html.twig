<form method="post" action="{{ path('gest_app_product_unit_delete', {'id': product_unit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_unit.id) }}">
    <button class="btn">Delete</button>
</form>
