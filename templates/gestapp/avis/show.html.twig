{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ avi.description }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ avi.author }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ avi.createdAt ? avi.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ avi.updatedAt ? avi.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('op_gestapp_avis_index') }}">back to list</a>

    <a href="{{ path('op_gestapp_avis_edit', {'id': avi.id}) }}">edit</a>

    {{ include('gestapp/avis/_delete_form.html.twig') }}
{% endblock %}
