{{ form_start(form) }}
<div class="card-body">
    <div class="row">
        <div class="col-12">
            <div class="row mb-1 mt-1 g-1">
                <div class="col-2">
                    {{ form_label(form.description, 'Description', {'label_attr': {'class':'form-label'}}) }}
                </div>
                <div class="col-10">
                    {{ form_widget(form.description, {'attr': {'class': 'form-control form-control-sm'}}) }}
                </div>
            </div>
            <div class="row mb-1 mt-1 g-1">
                <div class="col-2">
                    {{ form_label(form.author, 'Auteur', {'label_attr': {'class':'form-label'}}) }}
                </div>
                <div class="col-5">
                    {{ form_widget(form.author, {'attr': {'class': 'form-control form-control-sm', 'placeholder':'Membre & structure'}}) }}
                </div>
            </div>
        </div>
    </div>
</div>
<div class="card-footer">
    <a class="btn btn-sm btn-secondary" href="{{ path('op_Gestapp_avis_index') }}">Retour à la liste</a>&nbsp;<button class="btn btn-sm btn-primary">{{ button_label|default('Save') }}</button>
</div>


{{ form_rest(form) }}
{{ form_end(form) }}
