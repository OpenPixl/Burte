<div id="navbar">
    <div id="navbar_large" class="container{% if parameter.isBlocMenuFluid == 1 %}-fluid{% else %}{% endif %} d-lg-block d-none">
        <div class="row">
            <div class="col-3">
                {% if parameter.logoName %}
                    <img class="img-fluid" src="{{ vich_uploader_asset(parameter, 'logoFile') }}" alt="{{ parameter.logoName }}" style="height: 50px;">
                {% else %}
                    <img class="img-fluid" src="{{ asset('images/svg/user.svg') }}" alt="logo_OpenGaia" style="height: 50px;">
                {% endif %}
            </div>
            <div class="col-9">
                <nav class="navbar navbar-expand-lg navbar-light">
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler39" aria-controls="" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarToggler39">
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" href="{{ path('op_webapp_public_homepage')}}"> Accueil</a>
                            </li>
                            {% for menu in menus %}
                                <li class="nav-item">
                                    <a class="nav-link" aria-current="page" href="{{ path('op_webapp_page_slug', {'slug':menu.slug}) }}">{{ menu.name }}</a>
                                </li>
                            {% endfor %}
                            <li class="nav-item">
                                <a class="nav-link" aria-current="panier" href="{{ path('op_webapp_cart_showcart')}}">
                                    <i class="fas fa-shopping-cart"></i>
                                    Panier ({{ cartService.total }})
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" href="{{ path('op_admin_dashboard_index')}}"><i class="fas fa-unlock-alt"></i> Mon espace</a>
                            </li>
                        </ul>
                    </div>
                </nav>
            </div>
        </div>
    </div>
    <div id="navbar_small" class="container{% if parameter.isBlocMenuFluid == 1 %}-fluid{% else %}{% endif %} d-lg-none">
        <div class="row">
            <div class="col-3">
                {% if parameter.logoName %}
                    <img class="img-fluid" src="{{ vich_uploader_asset(parameter, 'logoFile') }}" alt="{{ parameter.logoName }}" style="height: 50px;">
                {% else %}
                    <img class="img-fluid" src="{{ asset('images/svg/user.svg') }}" alt="logo_OpenGaia" style="height: 50px;">
                {% endif %}
            </div>
            <div class="col-9">
                <nav class="navbar navbar-expand-lg navbar-light">
                    <button class="ms-auto navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarToggler39" aria-controls="" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarToggler39">
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" href="{{ path('op_webapp_public_homepage')}}"> Accueil</a>
                            </li>
                            {% for menu in menus %}
                                <li class="nav-item">
                                    <a class="nav-link" aria-current="page" href="{{ path('op_webapp_page_slug', {'slug':menu.slug}) }}">{{ menu.name }}</a>
                                </li>
                            {% endfor %}
                            <li class="nav-item">
                                <a class="nav-link" aria-current="panier" href="{{ path('op_webapp_cart_showcart')}}">
                                    <i class="fas fa-shopping-cart"></i>
                                    Panier ({{ cartService.total }})
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" aria-current="page" href="{{ path('op_admin_dashboard_index')}}"><i class="fas fa-unlock-alt"></i> Mon espace</a>
                            </li>
                        </ul>
                    </div>
                </nav>
            </div>
        </div>
    </div>
</div>
{% if route == 'op_webapp_public_homepage' %}
{% if parameter.sectionHome == 1 %}
<div id="header_home">
    <div id="bloc_prez" class="container" data-aos="fade-up">
        <div class="row">
            <div id="overlay_logo" class="col-md-6 offset-md-3 d-none d-md-block">
                <p class="text-center"> <img class="img-fluid" src="{{ asset('images/logosite_front/logo-fondblanc-614610a0b4fc4229405629.png') }}" alt="carte_de_priere" height="200px"> </p>
            </div>
        </div>
    </div>
    <div id="bloc_production3" class="container" data-aos="fade-down">
        <div class="row">
            <div class="col-lg-3 col-md-3 col-sm-12">
                <a href="#calligraphie" class="btn btn-light">Calligraphie</a>
            </div>
            <div class="col-lg-3 col-md-3 col-sm-12">
                <a type="button" class="btn btn-light" href="#aquarelles">Aquarelle</a>
            </div>
            <div class="col-lg-3 col-md-3 col-sm-12">
                <a type="button" class="btn btn-light" href="#artisanats">Autres artisanats</a>
            </div>
            <div class="col-lg-3 col-md-3 col-sm-12">
                <a type="button" class="btn btn-light" href="#creations">Vos créations</a>
            </div>
        </div>
    </div>
</div>
{% else %}
{% endif %}
{% endif %}
