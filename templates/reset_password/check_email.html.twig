{% extends 'admin.html.twig' %}

{% block title %}OpenGaia | Renouvellement du mot de passe{% endblock %}

{% block content %}
    <section class="container">
        <div class="row">
            <div class="col-12">
                <h1 class="mt-3 mb-3">Renouvellement du mot de passe :</h1>
                <p class="mt-3 mb-3">
                    Un email contenant le lien de renouvellement vous a été envoyé.
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p class="mt-3 mb-3">
                    Si vous n'avez pas reçu ce mail, pensez à vérifiez dans les spams de votre boite aux lettres où cliquez sur le lien suivant :
                    <br>
                    <a class="btn btn-sm btn-primary" href="{{ path('op_security_forgot_password_request') }}">essayez à nouveau</a></p>
            </div>
        </div>
    </section>
{% endblock %}
