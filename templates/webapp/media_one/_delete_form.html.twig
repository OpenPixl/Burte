<form method="post" action="{{ path('webapp_media_one_delete', {'id': media_one.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ media_one.id) }}">
    <button class="btn">Delete</button>
</form>
