{% extends 'base.html.twig' %}

{% block title %}EventGal{% endblock %}

{% block body %}
    <h1>EventGal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ event_gal.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ event_gal.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ event_gal.slug }}</td>
            </tr>
            <tr>
                <th>Alt</th>
                <td>{{ event_gal.alt }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ event_gal.description }}</td>
            </tr>
            <tr>
                <th>ImageName</th>
                <td>{{ event_gal.imageName }}</td>
            </tr>
            <tr>
                <th>ImageSize</th>
                <td>{{ event_gal.imageSize }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ event_gal.createAt ? event_gal.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ event_gal.updateAt ? event_gal.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('op_gestapp_eventgal_index') }}">back to list</a>

    <a href="{{ path('op_gestapp_eventgal_edit', {'id': event_gal.id}) }}">edit</a>

    {{ include('gestapp/event_gal/_delete_form.html.twig') }}
{% endblock %}
